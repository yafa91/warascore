import { View, Text } from "react-native"

export default function FavorisPage() {

    return (
        <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
            <Text style={{ fontSize:36}}>page de fav</Text>
        </View>
    )
}